<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Fill color: thay bằng color resource nếu muốn -->
    <solid android:color="#F0F0F0"/>

    <!-- Corner radius -->
    <corners android:radius="6dp"/>

    <!-- Optional stroke -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0"/>

    <!-- Optional padding (shape padding supported on some API levels) -->
    <padding
        android:left="6dp"
        android:top="2dp"
        android:right="6dp"
        android:bottom="2dp"/>
</shape>
